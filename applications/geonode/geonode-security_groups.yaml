- name: "geonode_secgroup"
  <% if $vpc_name %>
  vpc_name: "<%=$vpc_name %>"
  <% else %>
  vpc_name: "<%=$appname %>-vpc" 
  <% end %>
  rules: 
  - port: 80
    lbs: 
    - "geonode-lb"
  dependencies: 
  - type: "loadbalancer"
    name: "geonode-lb"
